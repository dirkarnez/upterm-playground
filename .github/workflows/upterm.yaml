name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - shell: bash
      run: ./upterm.sh

    - run: |
        while :
        do
        	echo Keep running
        	echo "Press CTRL+C to exit"
        	sleep 1
        done
